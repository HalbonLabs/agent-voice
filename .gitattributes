# Shell, Node and Python scripts must use LF so they run on macOS/Linux.
*.sh   text eol=lf
*.mjs  text eol=lf
*.py   text eol=lf
# Windows scripts keep CRLF.
*.ps1  text eol=crlf
*.cmd  text eol=crlf
